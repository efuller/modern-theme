var React = require('react');

var Header = React.createClass({
    render: function() {
        return (
            <div>
                <h1 className="site-title">React Front End Powered By WP-API</h1>
            </div>
        );
    }
});

module.exports = Header;